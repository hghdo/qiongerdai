<%= form_for(@admin_release) do |f| %>
  <% if @admin_release.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_release.errors.count, "error") %> prohibited this admin_release from being saved:</h2>

      <ul>
      <% @admin_release.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
