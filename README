Currently This rails project contains two main functions:
1) Crawl web pages from internet, fine tune the crawled article.
2) Show article or provide RSS for mobile clients.

Before it can crawl pages admin must config from where to crawl pages. To crawl page there are two steps:
1) Fetch content from configured source.
2) Fetch all images for crawled article.
